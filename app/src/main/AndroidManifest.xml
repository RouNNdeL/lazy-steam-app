<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.roundel.lazysteamhelper">

    <permission android:name="com.roundel.lazysteamhelper.permission.START_SERVICE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <service android:name=".NotificationReceiverService"
                 android:exported="true"
                 android:permission="com.roundel.lazysteamhelper.permission.START_SERVICE"/>
    </application>

</manifest>